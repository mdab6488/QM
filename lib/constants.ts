// App-wide defaults (not seed/dummy data — these are sensible starting values).

/** Default starting deposit suggested when creating a new group. */
export const DEFAULT_DEPOSIT = 250;

/** Default goal balance suggested for a new group/run. */
export const DEFAULT_GOAL = 30000;
